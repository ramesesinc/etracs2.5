
import com.rameses.annotations.*;
import com.rameses.http.*;

class SMSSendService 
{	
	@Service(value="SMSService", connection="etracsorg.sms")
	def smsSvc;

	@ProxyMethod
	@Async
	public def send( param) 
	{
		println "sending sms reply -> " + param;
		smsSvc.send(param);
	}		
}
