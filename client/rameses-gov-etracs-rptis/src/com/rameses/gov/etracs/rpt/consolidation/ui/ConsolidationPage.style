"entity.*|appraiser.*|recommender.*|taxmapper.*|approver.*" [allowEdit == true]{
    enabled:true;
}

"entity.*|appraiser.*|recommender.*|taxmapper.*|approver.*" [allowEdit != true]{
    enabled:false;
}