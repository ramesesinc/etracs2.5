<?xml version="1.0" encoding="UTF-8"?>
<workunit>
    <invokers>
        <invoker type="faasonline:info" action="init" caption="Requirements" index="500" />
        <invoker type="formActions" action="refresh" caption="Refresh" mnemonic="r" />
   </invokers>
   <code>

        
import com.rameses.rcp.annotations.* 
import com.rameses.rcp.common.* 
import com.rameses.osiris2.client.*
import com.rameses.osiris2.common.*

class FAASRequirementsInfoController
{
    @Binding
    def binding;

    @Service('RPTRequirementService')
    def reqSvc;
    
    def svc;
    
    String title = 'Requirements'
    
    def entity;
    def requirements;
    def selectedItem;
        
    void init(){
        requirements = reqSvc.getRequirements(entity.objid);
        listHandler?.load();
    }
    
    def getOpener(){
        if (!selectedItem)
            return null;
        def openers = InvokerUtil.lookupOpeners('rptrequirementtype:handler', [entity:selectedItem, svc:reqSvc]);
        return openers.find{it.properties.name == selectedItem.handler}
    }
    
    
    void refresh(){
        init();
    }
    
    
    def listHandler = [
        fetchList : { return requirements },
    ] as BasicListModel
    
}
       
   </code>
    
    <pages> 
        <page template="com.rameses.gov.etracs.rpt.faas.ui.FAASOnlineInfoRequirementPage"/>
    </pages>
</workunit>


