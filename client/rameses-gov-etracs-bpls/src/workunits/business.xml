<workunit>
    <invokers>
        <invoker type="business:create" caption="Capture Inactive Business" action="create" target="window" role="BUSINESSINFO,ASSESSOR,LICENSING"/>
        <invoker type="business:open" caption="Business Info" action="open" target="window" role="BUSINESSINFO,ASSESSOR,LICENSING"/>
        
        <invoker type="formActions" caption="Close" action="_close" visibleWhen="#{mode !='edit'}" immediate="true" />
        <invoker type="formActions" caption="Cancel" action="cancelEdit" visibleWhen="#{mode=='edit'}" immediate="true"/>
        <invoker type="formActions" caption="Save" action="save" visibleWhen="#{mode=='create' || mode == 'edit'}"/>
        <invoker type="formActions" caption="Edit" action="edit" visibleWhen="#{mode=='read' &amp;&amp; entity.state!='ACTIVE'}"/>
    </invokers>
    
    <code>
        <![CDATA[    
        import com.rameses.rcp.annotations.*
        import com.rameses.rcp.common.*
        import com.rameses.osiris2.client.*
        import com.rameses.osiris2.common.*
        import java.rmi.server.*
        import com.rameses.gov.etracs.bpls.business.*;
        
        class  BusinessInfoController extends AbstractBusinessController {
        
            @Service("BusinessInfoService")
            def service;
            
            String title = "Business Information";
            def mode = "read";
            def entityName = "business";
            
            def create() {
                entity = [objid: "BUS"+new UID(), lobs: []];
                entity.state = 'INACTIVE';
                mode="create";
                return "new";
            }
            
            def open() {
                entity = service.open(entity);
                mode="read";
                return "info";
            }
            
            def cancelEdit() {
                mode = 'read'
                return "info";
            }
            
            def edit() {
                mode = 'edit'
                return "edit";
            }
            
            def save() {
                if(mode == 'create') {
                    if(MsgBox.confirm("You are about to save this record. Proceed?")) {
                        entity = service.create(entity);
                    }
                }    
                else
                    entity = service.update(entity);
                    
                mode = "read";
                return "info";
            }
        }
        ]]>
        
    </code>
    <pages>
        <page name="new" template="com.rameses.gov.etracs.bpls.business.EditBusinessPage"/>
        <page name="edit"  template="com.rameses.gov.etracs.bpls.business.EditBusinessPage"/>
        <page name="info"  template="com.rameses.gov.etracs.bpls.business.BusinessInfoPage"/>
    </pages>
    
</workunit>