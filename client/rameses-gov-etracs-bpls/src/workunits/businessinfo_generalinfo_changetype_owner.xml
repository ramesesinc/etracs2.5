<workunit>
    <invokers>
        <invoker type="business:generalinfo:changetype" caption="Change Business Name" index="1" target="popup"/>
        
    </invokers>
    
       <code>
        <![CDATA[    
        import com.rameses.rcp.annotations.*
        import com.rameses.rcp.common.*
        import com.rameses.osiris2.client.*
        import com.rameses.osiris2.common.*
        import java.rmi.server.*
        import com.rameses.gov.etracs.bpls.business.*;
        
        class  ChangeBusinessOwnerController  {
        
            @Binding
            def binding;

            
            def newentity = [:]
            def entity;
            def handler;
            
            def doCancel() {
                return "_close";
            }
            
            def getLookupOwners() {
                if( !entity.orgtype ) {
                    MsgBox.err("Please select an orgtype first.");
                    return null;
                }    
                return InvokerUtil.lookupOpener( "entity:lookup", [
                    "query.orgtype": entity.orgtype,
                    onselect: { o->
                        entity.owner = o;
                        binding.refresh("entity.owner.address");
                    }
                ]);
            }
            
            def doOk() {
                handler(entity);
            }
        }
        ]]>
    </code>

    
    <pages>
        <page  template="com.rameses.gov.etracs.bpls.business.amend.ChangeBusinessOwner"/>
    </pages>
    
</workunit>