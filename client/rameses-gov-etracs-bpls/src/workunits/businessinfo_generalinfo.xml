<workunit>
    <invokers>
        <invoker type="business:info" caption="General Info" index="-100" 
            visibleWhen="#{entity.application?.objid==null || entity.application.task?.objid==null}"/>
    </invokers>
    
    <code>
        <![CDATA[    
        import com.rameses.rcp.annotations.*
        import com.rameses.rcp.common.*
        import com.rameses.osiris2.client.*
        import com.rameses.osiris2.common.*
        import java.rmi.server.*
        import com.rameses.gov.etracs.bpls.business.*;
        
        class  BusinessGeneralInfoController  {
        
            @Service("BusinessInfoService")
            def svc;
        
            @Binding
            def binding;
            
            String title = "General Info";
            String entityName = "businessinfo:generalinfo";
            
            def entity;
            
            def showChangeTypes(def inv) {
                def list = InvokerUtil.lookupOpeners("business:generalinfo:changetype", [entity:entity]);
                def popupMenu = new PopupMenuOpener();
                list.each {
                    popupMenu.add(it); 
                }
                return popupMenu;
            }
            
            def lobModel = [
                fetchList: { o->
                    return entity.lobs;
                }
            ] as BasicListModel;
            
        }
        ]]>
    </code>

    
    <pages>
        <page  template="com.rameses.gov.etracs.bpls.business.BusinessGeneralInfo"/>
    </pages>
    
</workunit>